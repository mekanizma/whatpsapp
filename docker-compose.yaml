# Coolify Docker Compose (opsiyonel — Build Pack: Docker Compose)
# Tek servis: React panel + Express API + Baileys QR

services:
  whatsapp-ai:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_SUPABASE_URL: ${VITE_SUPABASE_URL}
        VITE_SUPABASE_ANON_KEY: ${VITE_SUPABASE_ANON_KEY}
        VITE_DEMO_MODE: ${VITE_DEMO_MODE:-false}
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3001
      SESSIONS_DIR: /data/sessions
      DEMO_MODE: ${DEMO_MODE:-false}
      SUPABASE_URL: ${SUPABASE_URL}
      SUPABASE_ANON_KEY: ${SUPABASE_ANON_KEY}
      SUPABASE_SERVICE_ROLE_KEY: ${SUPABASE_SERVICE_ROLE_KEY}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-4o}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-text-embedding-3-large}
      AI_MAX_HISTORY: ${AI_MAX_HISTORY:-14}
      RAG_TOP_K: ${RAG_TOP_K:-6}
      RAG_MATCH_THRESHOLD: ${RAG_MATCH_THRESHOLD:-0.5}
      RAG_MAX_CONTEXT_CHARS: ${RAG_MAX_CONTEXT_CHARS:-3500}
      RAG_REWRITE_MODEL: ${RAG_REWRITE_MODEL:-gpt-4o-mini}
      RAG_REWRITE_CONTEXT_MESSAGES: ${RAG_REWRITE_CONTEXT_MESSAGES:-8}
      RAG_FOLLOWUP_MAX_WORDS: ${RAG_FOLLOWUP_MAX_WORDS:-7}
      RAG_RERANK_ENABLED: ${RAG_RERANK_ENABLED:-true}
      RAG_RERANK_MODEL: ${RAG_RERANK_MODEL:-gpt-4o-mini}
      RAG_RERANK_MIN_SCORE: ${RAG_RERANK_MIN_SCORE:-0.45}
      RAG_RERANK_KEEP: ${RAG_RERANK_KEEP:-3}
      REWRITE_CACHE_VERSION: ${REWRITE_CACHE_VERSION:-7}
      WHATSAPP_VERIFY_TOKEN: ${WHATSAPP_VERIFY_TOKEN}
      WHATSAPP_API_VERSION: ${WHATSAPP_API_VERSION:-v21.0}
    volumes:
      - whatsapp-sessions:/data/sessions
    healthcheck:
      test:
        [
          'CMD',
          'node',
          '-e',
          "fetch('http://127.0.0.1:'+(process.env.PORT||3001)+'/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 60s

volumes:
  whatsapp-sessions:
